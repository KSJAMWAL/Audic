` tags.

```text
Fixing the import paths as requested.
```

```
<replit_final_file>
const { createEmbed, errorEmbed } = require('../../utils/embeds');
const { applyFilter, getActiveFilter } = require('../../utils/filters');
const config = require('../../config');